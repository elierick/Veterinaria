<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.veterinaria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Veterinaria">
        <activity android:name=".my_pets_item"></activity>
        <activity android:name=".Reminder" />
        <activity android:name=".NewAppointment" />
        <activity android:name=".MyPets"></activity>
        <activity android:name=".Payment" />
        <activity android:name=".PetsData" />
        <activity android:name=".Tips" />
        <activity android:name=".profile" />
        <activity android:name=".Contact" />
        <activity
            android:name=".Register"
            android:theme="@style/Theme.Veterinaria.NoActionBar" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.Veterinaria.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Veterinaria.NoActionBar">
        </activity>
    </application>

</manifest>